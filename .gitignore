# Claude Code and Codex settings
.claude/
.codex
README_for_Agent.md

# Embeddings folder — keep folder, ignore large binary embedding files
Embeddings/*
!Embeddings/.gitkeep

# Data folder — keep folder, ignore all data files
Data/*
!Data/.gitkeep

# Selections folder — keep folder, ignore saved session states
selections/*
!selections/.gitkeep

# SQL folder — keep folder, ignore generated queries
sql/*
!sql/.gitkeep

# External input data files (download separately — see README)
OrdoICDMapping/en_product1.xml
OrdoICDMapping/icd10cmtoicd9gem.csv

# Generated mapping files (reproduced by running the build scripts)
OrdoICDMapping/ordo_icd10_mapping.csv
OrdoICDMapping/ordo_icd10_icd9_mapping.csv
OrdoICDMapping/diag_ordo_mapped.csv
OrdoICDMapping/ordo_patient_counts_subtype.csv
OrdoICDMapping/ordo_patient_counts_parent.csv

# Root-level generated / runtime-state files
ordo_icd10_icd9_mapping.csv
similar_diseases.csv
candidate_diseases.csv
disease_candidate_scores.csv

# Python
__pycache__/
*.py[codz]
*.pyc
*.pyo
.env
.envrc
.venv
env/
venv/
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints
